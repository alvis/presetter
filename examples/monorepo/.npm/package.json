{
  "name": "example-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "coverage": "run coverage",
    "install": "presetter bootstrap --projects . --projects */*/",
    "lint": "run lint -- packages",
    "prepare": "husky || true",
    "test": "run test --",
    "watch": "run watch --"
  },
  "devDependencies": {
    "@presetter/preset-monorepo": "*",
    "@presetter/preset-next": "*",
    "@types/node": "*",
    "presetter": "*",
    "typescript": "*"
  }
}
