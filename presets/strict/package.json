{
  "name": "@presetter/preset-strict",
  "version": "9.0.1",
  "description": "An extension of the cjs/esm preset to provide stricter lint rules for typescript projects",
  "keywords": [
    "presetter",
    "preset"
  ],
  "homepage": "https://github.com/alvis/presetter#readme",
  "bugs": {
    "url": "https://github.com/alvis/presetter/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alvis/presetter.git"
  },
  "license": "MIT",
  "author": "Alvis HT Tang <alvis@hilbert.space>",
  "type": "module",
  "imports": {
    "#*": "./src/*.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "bootstrap": "presetter bootstrap",
    "build": "run build",
    "lint": "run lint --",
    "prepare": "tsc --rootDir src --ignoreConfig --types node --skipLibCheck --declaration --moduleResolution bundler --module esnext --target esnext --skipLibCheck --outdir lib src/index.ts && tsc-esm-fix --sourceMap --target lib",
    "prepublishOnly": "run prepare && run prepublishOnly",
    "test": "run test --",
    "test:coverage": "run test:coverage --",
    "test:watch": "run test:watch --",
    "typecheck": "run typecheck --"
  },
  "dependencies": {
    "@presetter/types": "workspace:*"
  },
  "peerDependencies": {
    "eslint-plugin-no-secrets": "catalog:lint",
    "eslint-plugin-sonarjs": "catalog:lint",
    "sort-package-json": "catalog:lint"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "publishConfig": {
    "access": "public",
    "exports": {
      ".": "./lib/index.js"
    },
    "imports": {
      "#*": "./lib/*.js"
    }
  }
}
