{
  "preset": "presetter-preset-strict",
  "config": {
    "jest": {
      "coverageReporters": [
        "text",
        [
          "lcov",
          {
            "projectRoot": "{root}"
          }
        ]
      ]
    },
    "npmignore": ["!/configs/**", "!/templates/**"]
  },
  "variable": {
    "root": "../.."
  }
}
