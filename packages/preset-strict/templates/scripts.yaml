lint:eslint: eslint --fix --format codeframe --max-warnings 0 --no-error-on-unmatched-pattern --ext js,jsx,ts,tsx
prepare: run-s setup build
setup: run-s setup:*
setup:husky: (husky install && husky set .husky/pre-push "npx lint-staged --concurrent=false") || true
